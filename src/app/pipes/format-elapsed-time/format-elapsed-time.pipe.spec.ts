import { FormatElapsedTimePipe } from './format-elapsed-time.pipe';

describe('FormatElapsedTimePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatElapsedTimePipe();
    expect(pipe).toBeTruthy();
  });
});
